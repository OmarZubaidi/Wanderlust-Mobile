import colors from './colors';

export default {
  dark: false,
  colors: {
    primary: colors.blue,
    background: colors.white,
    card: colors.blue,
    text: colors.black,
    border: colors.white,
    notification: colors.blue,
  },
};
